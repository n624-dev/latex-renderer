import { describe,expect,it } from "vitest";
import app from "../apps/gateway-worker/src/index.js";
const env={INTERNAL_API:{fetch:()=>Promise.resolve(new Response(null,{status:500}))}};
describe("unified gateway routes",()=>{it("rejects invalid ids on canonical renewal route",async()=>{const response=await app.request("/api/v1/job-tickets/nope",{method:"POST",headers:{Authorization:"Bearer lrk_11111111111111111111111111111111_1111111111111111111111111111111111111111111","Content-Type":"application/json","Content-Length":"2"},body:"{}"},env);expect(response.status).toBe(400);});it("rejects unsupported methods before reaching the private API",async()=>{expect((await app.request("/api/v1/render-tickets",{method:"GET"},env)).status).toBe(405);});it("does not expose upload routes",async()=>{expect((await app.request("/api/v1/jobs/job_00000000000000000000000000000000/source",{method:"PUT"},env)).status).toBe(404);});});
